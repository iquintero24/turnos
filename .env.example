# Archivo .env.example
# 🚨 NO colocar credenciales reales aquí.
# Copiar este archivo como '.env' y rellenar los valores reales.

# Cadena de conexión principal que usa Program.cs
SUPABASE_CONNECTION_STRING="Host=<TU-HOST-POOLER>;Port=5432;Database=<TU-BD>;Username=<TU-USUARIO>;Password=<TU-CONTRASEÑA>;Pooling=True;MinPoolSize=1;MaxPoolSize=5"

# Conexión para herramientas administrativas (migraciones EF Core)
SUPABASE_MIGRATION_CONNECTION="Host=<TU-HOST-DIRECTO>;Port=5432;Database=<TU-BD>;Username=<TU-USUARIO-MIGRACION>;Password=<TU-CONTRASEÑA-MIGRACION>"

# --- OTRAS VARIABLES DE ENTORNO ---
AMBIENTE=Desarrollo